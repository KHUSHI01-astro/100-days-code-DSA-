#include <stdio.h>

int main() {
    int n, k;
    int i;
    int comparisons = 0;
    int found = 0;

    
    scanf("%d", &n);

    int arr[n];

    // Input array elements
    for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Input key
    scanf("%d", &k);

    // Linear Search
    for (i = 0; i < n; i++) {
        comparisons++;              // count
